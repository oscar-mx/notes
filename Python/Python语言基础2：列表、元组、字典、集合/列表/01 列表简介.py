# 创建一个列表，只要把逗号分隔的不同的数据项使用方括号括起来即可。
list1 = ['mx', 'ly', 'love', 520]
print(type(list1))
print(list1)

# 使用下标索引来访问列表中的值，同样你也可以使用方括号的形式截取字符 
# 索引从0开始
print(list1[0])

# 列表长度 len(list) 函数，获取到的长度值是列表的最大索引 + 1
print (len(list1))